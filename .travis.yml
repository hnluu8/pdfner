sudo: false

language: python
python:
  - "3.7"

services:
  - elasticsearch

install:
  - pip install -r requirements.txt

stages:
  - test

jobs:
  include:
    - stage: test
      before_script:
      - sleep 10
      script:
      - pytest --verbose --capture=no pdfner/tests/test_search.py

notifications:
  email: false


