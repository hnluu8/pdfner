sudo: false

language: python
python:
  - "3.7"

before_install:
  - curl -O https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-6.8.6.deb && sudo dpkg -i --force-confnew elasticsearch-6.8.6.deb && sudo service elasticsearch start

install:
  - pip install -r requirements.txt

stages:
  - test

jobs:
  include:
    - stage: test
      before_script:
      - sleep 10
      script:
      - pytest --verbose --capture=no pdfner/tests/test_search.py

notifications:
  email: false


